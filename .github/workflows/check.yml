name: 'Check'

on:
  workflow_dispatch:
  schedule:
  - cron:  '0 * * * *'

jobs:
  check:
    runs-on: self-hosted
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    - name: Run script
      env:
        TOKEN: ${{ secrets.TOKEN }}
      run: python3 steam.py
